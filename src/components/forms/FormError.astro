---
import type { FormError as Props } from '~/types';

const { error = undefined } = Astro.props;
---

{
  error && (
    <div class="mb-6 px-5">
      <span class="text-red-500 text-s italic">{error.message}</span>
    </div>
  )
}
